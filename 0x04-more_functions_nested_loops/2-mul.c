#include "main.h"

/**
 * mul - A function to multiply two integers
 * @a : Integer to be multiplied
 * @b : Integer to be multiplied
 * Return: Always
 */

int mul(int a, int b)
{
	return (a * b);
}
