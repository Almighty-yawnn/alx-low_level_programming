#include <stdio.h>

/**
 * main - A program that prints the alphabet in lowercase
 * Return: Always
 *
 */
int main(void)
{
	char h;

	for (h = 'a' ; h <= 'z' ; h++)
	putchar(h);
	putchar('\n');
	return (0);
}
